# --- Backend (Render: set these in the service Environment tab) ---
MONGO_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/studyapp?retryWrites=true&w=majority
JWT_SECRET=your-long-random-secret-here
PORT=5000
# Comma-separated frontend URLs allowed by CORS
CLIENT_URL=https://study-app-liart.vercel.app,http://localhost:3000

# --- Frontend (Vercel: Project Settings → Environment Variables) ---
# No trailing slash
VITE_API_URL=https://study-app-1-dyv3.onrender.com
